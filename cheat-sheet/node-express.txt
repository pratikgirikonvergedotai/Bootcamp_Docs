what is node js ?
Node.js is an open-source, cross-platform, back-end JavaScript runtime environment that runs on the V8 engine and 
executes JavaScript code outside a web browser


it has gloabl variable instead of window.

it has some modules like os,fs,path,http 

_dirname - path to current directory
_filename - file name



express : its a framework for node js 

const express  = require("express")
const app = express()



app.use(// we can use any middlware here and we can setup router here too)
app.use(express.urlencoded({ extended: false }))
app.use(express.json())



// we can  use all http methods like get, post, put, delete
app.get()

app.post()

app.put()

app.delete()


app.listen()


res.json() // json as response
res.send() // send a response
res.render() // render template 


// route params
/api/v1/:id 
req.params.id


// query string
/api/v1?name=nodejs&number=9
req.query.name , req.query.number




// data in request object
--> req.body.name 


// callbacks 

function exmaple_callback(variable, callback){
    callback(variable)
}



// promises

promise either resolves or reject no in between 

we can create a promise as :

let newPromise = new Promise((reject, resolve) => {
    // something here for promise 
})

// handling promise. then handles promise if resloved . catch handles error if promise gets rejected
newPromise().then().catch()

// async await 

async await is nice way of writing promise with then and catch

use async and await must be used together

async function connect(){
    await client.connect()
}



